# {{ ansible_managed }}
MAILTO=root

{% for instance in tomcat_instance %}
53 01 * * * {{ tomcat_system_user }} find {{ tomcat_system_home }}{{ instance.instance_name }}/logs \( -name \*.current -or -name \*.log -or -name \*.txt \) -type f -mtime +2 -print0 | xargs -0 -t -r xz -9 -e -v
{% endfor %}
